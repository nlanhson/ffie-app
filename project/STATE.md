# Project State

**Project:** FFIE — Fédération Française des Intégrateurs Électriciens — mobile app (iOS + Android)
**Started:** 2026-05-28
**Mode:** instructor
**Current stage:** 2 → 3 transition — Stage 2 design system foundation done; 6 components shipped. **Scope policy (2026-05-28):** The product is a React Native mobile app for iOS + Android, built under `./mobile/`. The Next.js site under `./design-system-preview/` is **purely design-system documentation** — tokens, foundations, components. Screens belong in `./mobile/` only. (tokens.ts v0.6; preview site ✓ all foundation + component routes 200; **mobile app shell ✓** at ./mobile — Expo SDK 56 + RN 0.85, tokens.ts imported via Babel alias + Metro watchFolders; 6 components ✓ Input v1.0, Button v1.0, StatusPill v1.0 (web + RN), Modal v1.0, Toast v1.0, Card v1.0; **mobile screens ✓ Doc Library v1.0, Splash v1.0**; next: Screen 2 Doc Detail in `./mobile/`)

## One-line summary
Native iOS + Android app for a French professional electrical-integrators federation: members get a news feed + secure document library, the general public gets a "discover the trades" section, FFIE admins get a back-office. Phase 1 in 4–5 months from 23/04/2026 signature.

## Decisions so far
- 2026-05-29 — **Guest sign-in flow + debug reset.** (1) **"I already have an account"** now opens a real email → OTP login instead of instantly flipping the role → new `src/screens/auth/SignInFlow.tsx` reuses the onboarding login screens verbatim (`EmailEntryScreen` → `OtpEntryScreen`, stacked slide-up modals, OTP over email). Wired in `App.tsx` GuestShell: `onSignIn` → `openSignIn` (was `signInAsMember`); a completed 6-digit code calls `authenticate()` → closes the modal + `setRole("member")`. Same behaviour as the Welcome-screen login; v1 mock accepts any 6-digit code (production: verify against FFIE auth API). (2) **Debug reset** added to `RoleDebugSwitcher` — a "Reset" chip (RotateCcw) left of the role chip that calls `membershipContext.withdrawApplication()` to wipe the logged membership application (disabled/dimmed when `status === "none"`); lets the client re-test the apply→pending loop without relaunching. **Scoped to the Join-FFIE tab only:** new `src/navigation/activeTabContext.tsx` (`ActiveTabProvider` wraps app under `MembershipProvider`) — `GuestShell` publishes its `activeTab` via a `useEffect` (clears to null on unmount), and the floating switcher (a root sibling that can't see shell-local tab state) reads it; the Reset chip renders only when `activeTab === "become-member"`. `npx tsc --noEmit` clean. Open: SignInFlow `onResend` is a no-op stub; reset currently only clears the membership application (extend if more session data gets logged).
- 2026-05-29 — **Membership application flow (Karim) — submit → pending state** → new `src/auth/membershipContext.tsx` + `src/screens/membership/{MembershipApplicationFlow,MembershipApplicationScreen,ApplicationSubmittedScreen}.tsx`; wired into `App.tsx` (GuestShell) + pending banner added to `BecomeMemberScreen.tsx`. Fills the previously-stubbed `onApply` (App.tsx `handleApply` was a no-op). **Lead-capture model honored** (PRD payments boundary): no in-app payment, application is recorded and enters a `pending` state; real membership granted out-of-band by FFIE. **Funnel:** News/member-only upsell → Join value page (`BecomeMemberScreen`) → "Apply" CTA opens the application as a slide-up `Modal` over the guest shell (same pattern as Notifications; fresh `SafeAreaProvider`). **State machine** (`MembershipApplicationFlow`): form → submitted/pending; seeds straight to the confirmation screen if an application already exists. **Form** uses the RN `Input` + `Button` components: company name*, SIRET* (14-digit validation), contact name*, email* (regex), phone (optional), company-size segmented chips (optional, drives dues band), required eligibility consent checkbox; validation on submit with inline errors; 700ms simulated submit so the CTA shows its loading state. **Confirmation** = "Application received" (Clock, not checkmark — state is pending not done) with reference (`FFIE-YYYY-NNNN` mock) + submitted date + "we'll email you" + 3-step what-happens-next. **Persistence:** `MembershipProvider` (wraps app under `RoleProvider`) holds the submitted application in memory → `status: "pending"`; `BecomeMemberScreen` shows a pending banner (ref + email) and swaps its CTA to "View application status" (reopens the flow → confirmation screen). `npx tsc --noEmit` clean. **To test:** App currently boots as `member` (initialRole set by designer) → use the RoleDebugSwitcher (top-right) to switch to a guest role, then News/Join → Apply. Open: real FFIE membership API POST + SecureStore persistence (status survives relaunch, refresh on cold start); numeric keyboards for SIRET/phone (Input only maps email→email-address today — needs a `keyboardType`/`inputMode` prop); FR copy pass (all English); withdraw/edit-application path; richer status states (under-review / approved / rejected) when the API defines them.
- 2026-05-29 — **News category filter + shared filter component** → new [`mobile/src/components/ui/FilterControls.tsx`](../mobile/src/components/ui/FilterControls.tsx); wired into [`NewsScreen.tsx`](../mobile/src/screens/NewsScreen.tsx) and refactored [`DocLibraryScreen.tsx`](../mobile/src/screens/DocLibraryScreen.tsx). Designer asked for a way to "sort the type of news," matching the Library filter's visual style. **Rather than copy the look, extracted the Library's filter UI into a shared, generic-over-`K` component** so both screens are now literally the same code: `FilterButton` (38×38 rounded `SlidersHorizontal` trigger, brand-accent fill + red count badge when active) and `FilterSheet` (animated bottom sheet — scrim fade + sheet slide on the native driver, drag handle, display-font title, toggleable chips, Reset / "Show N results", immediate-apply, tap-outside dismiss). DocLibrary lost ~250 lines of inline sheet/styles and now imports the shared component (`sectionLabel="Status"`, `options={FILTER_OPTIONS}`) — behaviour identical, type-check clean. News uses `sectionLabel="Category"` with the 4 categories from the updated `news.ts` union (Technical / Training / Communication / Economical), multi-select (empty set = show all), filtered via `useMemo`; the `FilterButton` sits in the `LargeTitleHeader` trailing slot (iOS large-title accessory). Filtered list re-derives hero + 2-col grid; added an empty-state ("No articles in this category"). **Note:** `news.ts` categories were changed by the designer from the old Federation/Standards/Sector/Events set to Technical/Training/Communication/Economical — `CATEGORY_OPTIONS` tracks the new union. Open: FR copy pass (categories, "News", "min read", "Members", filter labels are still English — app canonical language is French); consider a `featured` flag for the hero; FlashList when the feed grows.
- 2026-05-29 — **News feed layout reworked → hero + 2-column grid** → [`mobile/src/screens/NewsScreen.tsx`](../mobile/src/screens/NewsScreen.tsx). Mirrors the FFIE web news page (designer-supplied BuzzFeed reference: one highlighted item, rest in a 2-col grid). Replaced the single-column full-width card stack with: **HeroCard** = latest article (`ARTICLES[0]`), full-width, 16:9 `RemoteImage`, display-font (Sora) headline at 23/29 — the one "feature" moment, echoing `LargeTitleHeader`; **GridCard** = `ARTICLES.slice(1)` in two equal columns (width computed from `useWindowDimensions`: `(screenW - GUTTER*2 - COL_GAP)/2`, COL_GAP 14 / ROW_GAP 18 via flexWrap), 4:3 image, Raleway-700 headline clamped to 3 lines so column heights stay even. Extracted two shared helpers — `CategoryTag` (colour **+** uppercase label, `muted` = locked treatment) and `LockTag` (Lock icon **+** "Members") — so colour-independence (P4) holds in both card sizes. **Preserved unchanged:** role gating via `canAccess`, member-only locked-teaser → `MemberOnlyPrompt` upsell, reader nav to `NewsArticleScreen`, single-`Pressable`-per-card / no-nested-focusable rule (carried from Doc Library), press feedback (bg tint + scale). `npx tsc --noEmit` clean. Open: real `featured` flag in `news.ts` instead of "hero = index 0"; FlashList virtualization once the feed grows past a screenful; FR copy pass (data/news.ts + "News"/"min read"/"Members" are still English — app canonical language is French); odd-count grid leaves a half-width last row (acceptable, matches web).
- 2026-05-28 — Mode set to instructor. Designer wants step-by-step explanations of what skills are running and why.
- 2026-05-28 — Brief read checkpoint: PRD v0.2 + 3 WBS CSVs (4 personas, 12 epics, 29 stories) ingested. PRD is structured but still has multiple TBCs (opportunity, member age, acceptance criteria for core US, post-Phase 2 evolutions).
- 2026-05-28 — Design brief written → [./brief/DESIGN_BRIEF.md](./brief/DESIGN_BRIEF.md). Includes 8 proposed KPIs (member MAU 60%, document task success 85% in <60s, news push open 35%, public reach 5k/6mo, depth 3+/session, admin zero-escalation 90d, WCAG 2.2 AA, offline 10 news + 5 docs) — all flagged "Proposed — to be validated with FFIE." 5 Open Questions surfaced for FFIE (pain validation, US-01/02/03 ACs, member-management API spec, brand identity, opportunity statement).
- 2026-05-28 — Personas written → [./brief/PERSONAS.md](./brief/PERSONAS.md). 4 personas with names, day-in-the-life scenarios, ability/situational dimensions, and concrete design implications. Julien Marchand (47, FFIE member, electrical-integration company owner-manager in Tours) flagged PRIMARY. Persona-driven non-negotiables surfaced: offline-first for docs + last 10 news, search-as-front-door for doc library, 48pt touch targets, biometric login default, mandatory video captions, two-step push send with preview, per-doc analytics inline in back-office. Composite personas — research gaps logged for FFIE field validation.
- 2026-05-28 — Design principles written ✓ → [./brief/DESIGN_PRINCIPLES.md](./brief/DESIGN_PRINCIPLES.md). 7 opinionated, persona-anchored, testable principles ordered by precedence (lower number wins on conflict): (1) Field-ready over feature-rich [Julien — gloved/dead-zone/sun], (2) Offline is the default not the exception [Julien — rural worksite connectivity], (3) Search is the front door not a feature [Julien — reactive doc lookup], (4) Accessibility is a floor not a ceiling [WCAG 2.2 AA min, AAA body text; RGAA + ageing member base + situational impairments], (5) Forgive the editor even before the publisher [Sylvie — two-step destructive actions, 60s undo, status clarity], (6) No login wall on what the public should see [Karim + Léa — public discovery without account], (7) Show the value before asking for the relationship [Karim + Léa — substance over brochure copy]. Each principle has: one-line stance, rationale, in-practice examples, anti-patterns, and a single testable check. Sign-off block reserved for FFIE.
- 2026-05-28 — Scope audit performed across DESIGN_BRIEF.md, PERSONAS.md, DESIGN_PRINCIPLES.md, STATE.md against PRD out-of-scope list (payments/e-commerce, ticketing, in-app messaging/chat/DMs, forum, AI assistant, public API) and Phase 2 deferrals (tutorials, calculators). **0 hard out-of-scope items removed.** **0 rewrites.** **2 items flagged for FFIE confirmation:**
  - PERSONAS.md §Karim Goal #4 line 107 + Design Implication #3 line 132 — "fast path to apply" / "membership flow on mobile." Defensively scoped already to "capture lead + email and follow up" if FFIE backend can't accept digital applications. Stays on the lead-capture side of the payments boundary. **Confirm with FFIE: the membership apply flow is lead-capture only, no payment processing in-app.**
  - DESIGN_PRINCIPLES.md P7 line 190 — "See pricing & apply" CTA copy. Same boundary as above. Defensible if "apply" = inquiry/lead-capture; out-of-scope if "apply" = in-app purchase. **Confirm with FFIE.**
- 2026-05-28 — Scope audit: items kept as external-but-real (not out-of-scope):
  - PERSONAS.md §Julien lines 57, 64, 84 — "share to WhatsApp / Mail" — external apps, not in-app chat. PRD bans *in-app* messaging/chat between members; sharing *out* to user-installed apps is a standard mobile share-sheet behavior, in scope.
  - PERSONAS.md §Karim Goal #5 line 108 — "stay informed about the broader trade" → public news, explicitly in Phase 1 scope.
- 2026-05-28 — **Tech stack locked: TypeScript everywhere.** Mobile = React Native + Expo (single codebase, real native binary, satisfies PRD "native" requirement, supports offline cache + push + OS share for Julien's WhatsApp behavior). Back-office = Next.js + React. Rationale: 4–5 month deadline favors shared codebase; project skills bundle already includes `vercel-react-native-skills`, `vercel-react-best-practices`, `shadcn-ui`, `frontend-design`. **Confirm with FFIE** that "native" in PRD means experientially native (installable app, not browser), not technically native (Swift/Kotlin only).
- 2026-05-28 — **Stage 2 kickoff — Token taxonomy ✓** → [./design-system/TOKENS.md](./design-system/TOKENS.md). Skill: `design-systems--design-token`. Locked: 3-tier model (primitive → semantic → component), 9 categories (color, typography, spacing, sizing, radius, elevation, motion, opacity, breakpoints), naming convention (kebab in docs, dot-paths in TS exports), FFIE-specific tokens traced to principles P1–P7 (`target.primary`=48pt P1, `feedback.offline/syncing/stale` + `opacity.stale` P2, `state.gated.public/member-only` P6+P7, `action.destructive` + `feedback.undo.bg` + `motion.undo.duration`=60000ms P5, `theme.contrast-mode.sunlight` P1+P4). RN-specific decisions: unitless numbers (no rem/px strings), shadow tokens as `{ios, android, web}` structured objects, no CSS variables (theme-via-context), `allowFontScaling` never disabled (P4). Brand identity gap absorbed via labeled `brand.*` primitive slot — one-file swap when FFIE provides assets. Open questions logged: 4pt vs 8pt base, system vs custom sans, brand placeholder hue, sunlight as separate file vs runtime modifier.
- 2026-05-28 — **Canonical tokens module + live preview site ✓** → http://localhost:3000. Canonical token source is now [./design-system/tokens.ts](./design-system/tokens.ts) — TypeScript module exporting `{ primitives, themes }` with three themes (light, dark, sunlight) bound from primitive ramps. v0.1 placeholder values: cool-neutral gray ramp (50–950), indigo brand placeholder (~#3B5BDB family), red/amber/green/blue status ramps, 4pt space scale, 1.25 modular type scale, 44/48/56 touch targets, motion durations including the 60000ms undo window, structured `{ios, android, web}` elevation. Preview app at [../design-system-preview/](../../design-system-preview/) — Next.js 15 + React 19 + Tailwind 3.4, App Router, 10 routes: Overview, foundations/{color, typography, spacing, sizing, motion, radii, elevation}, themes (light/dark/sunlight switcher), tokens (flat searchable table). Preview imports `@tokens` directly via webpack alias + `experimental.externalDir`, so edits to `tokens.ts` hot-reload via Next Fast Refresh — the "always keep updated" mechanism is the import, not a sync step. Resolved TOKENS.md §9.4 open question: sunlight is a **separate semantic theme**, not a runtime modifier (cleaner review surface; matches mobile context-based theming). WCAG AAA/AA badges on the color page are computed at render time via WCAG 2.x relative-luminance math in `src/lib/contrast.ts` — no hardcoded labels. The upcoming `ui-design--color-system`, `--typography-scale`, `--spacing-system` skills will refine values in `tokens.ts` and the preview will reflect automatically without preview-app edits.
- 2026-05-28 — **Preview site rebuilt from scratch.** Source folder was lost (only `.next/` build cache remained). Rebuilt at [../design-system-preview/](../../design-system-preview/) with identical scope per this STATE — Next.js 15 + React 19 + Tailwind 3.4, App Router, `@tokens` external alias via `experimental.externalDir`, `src/lib/contrast.ts` render-time WCAG math. All 10 original routes + 1 new section (Flows, see below) render 200. Now running on **http://localhost:3001** (port 3000 held by an unrelated project at `/Users/du-mac/FFIE/design-system-preview/`).
- 2026-05-28 — **First user flow committed** → [../design-system-preview/src/app/flows/julien-find-doc/page.tsx](../../design-system-preview/src/app/flows/julien-find-doc/page.tsx). Skill: `prototyping-testing--user-flow-diagram`. Canonical scenario for primary persona Julien Marchand — "find a regulatory document on a worksite." 7-step critical path (~22s wall-clock, 60s budget) with two annotated branches off step 5 (Stale cache, Offline-uncached + recovery). Every step tagged with the design principles it honors (P1 field-ready, P2 offline-default, P3 search front door, P4 accessibility floor). Stress-tests biometric default, search-as-front-door, offline-first cache, status pills with shape+color (never colour alone), and the no-dead-end rule on the offline branch. Six "design contracts" listed below the diagram — slips on any one of them break the flow. Mounted at `/flows` (index) and `/flows/julien-find-doc`; nav updated.
- 2026-05-28 — **Brand identity resolved ✓** Open question §7.4 closed. Brand layer extracted from the live federation site at https://www.ffie.fr by parsing the logo SVG (`#222D5D` navy + `#009DDF→#006192` cyan radial gradient) and the compiled stylesheet's hex frequency (`#0094a9` × 58 — the primary CTA hue, `#02b5ce` × 47 secondary, `#04c6e2` × 14 highlight). tokens.ts v0.1 → v0.2: the indigo placeholder is removed; in its place a `primitives.colors.brand` namespace with `navy` (anchored at `brand.navy.700 = #222D5D`, the logo) and `teal` (anchored at `brand.teal.600 = #0094A9`, the ffie.fr primary CTA) tonal ramps. All three theme bindings (light, dark, sunlight) rebound: `action.primary` is brand.teal in light/dark and brand.navy[900] in sunlight (deeper for outdoor visibility). New semantic slots: `theme.brand.{institutional, accent}` and `text.brand`. Font stack extended with `brand` (Museo Sans + system fallback) and `display` (Montserrat + system fallback) — Museo is paid; FFIE webfont license still TBC, system stack renders by default. Logo asset copied to [../design-system-preview/public/logo-ffie.svg](../../design-system-preview/public/logo-ffie.svg) and shown in the preview nav header + Overview hero. Overview page now opens with a navy→teal gradient hero featuring the logo and the FFIE tagline "Construisons l'électricité, ensemble." TOKENS.md §9.3 (brand placeholder hue) resolved.
- 2026-05-28 — **Typography scale ✓** → [./design-system/TYPOGRAPHY.md](./design-system/TYPOGRAPHY.md). Skill: `ui-design--typography-scale`. tokens.ts v0.2 → v0.3. Added two new primitives: `letterSpacings` (unitless em multipliers — tighter / tight / normal / wide / wider) and `textStyles` (15-role semantic mapping — display.lg/md, heading.h1-h4, body.lg/md/sm, label.lg/md/sm, eyebrow, caption, code). Components reference roles by name; the underlying primitive composition is encapsulated. Type-scale audit recorded: scale is ~1.25 modular with drift at xs→sm (-6.7%) and 3xl→4xl (+6.7%) because we round to safe whole-number sizes. Kept as-is — matches Tailwind/Material/Carbon convention and reads predictably at every step. **Montserrat now loaded via `next/font/google`** in the preview (latin + latin-ext subsets, weights 400/500/600/700) — self-hosted, no FOUT, no runtime Google call. globals.css exposes `--font-display` / `--font-brand` / `--font-mono` CSS variables that the typography page consumes. Museo Sans remains license-TBC; brand stack falls through to system. Rebuilt [/foundations/typography](http://localhost:3001/foundations/typography) with: (a) role table — every textStyle rendered with a real French specimen, (b) **Dynamic-Type simulator** — 4 scale toggles (100% / 115% iOS+1 / 135% iOS+3 / 200% WCAG 1.4.4) that scale every specimen live so we can spot overflow + readability issues at the OS-scaling boundaries, (c) primitive views for sizes / weights / line-heights / letter-spacings / families with FOUT-aware status badges (next/font · green; Museo TBC · amber). Four design contracts surfaced: 200% must read, allowFontScaling defaults true, body floor 16pt, uppercase requires wider tracking. Open items: Museo licensing decision; real-device testing on Julien's phones at OS-max scaling; French-specific tracking review on display roles (longer words + accent density); RN consumption helper for em→points conversion.
- 2026-05-28 — **Color system refinement ✓** → [./design-system/COLOR_SYSTEM.md](./design-system/COLOR_SYSTEM.md). Skill: `ui-design--color-system`. Three-level audit built into the preview at /foundations/color, all computed at render-time from tokens.ts (no separate sync): (1) Ramp audit — CIE L* progression per step, mean ΔL* and stdev surfaced; Tailwind ramps and brand.navy show normal mid-range unevenness (~stdev 3–4.5), brand.teal is the most evenly stepped (stdev 2.65). Decision: do NOT re-tune the Tailwind ramps cosmetically — semantic bindings are what's tested. (2) Cross-theme contrast catalog — 33 pairs (light/dark/sunlight × text/action/feedback/border). **Status: 33/33 pass.** Three real bugs fixed during the pass: `light.action.primary.bg` brand.teal[600]→[700] (white-on-teal 3.62→5.66:1), `light.feedback.success` green[600]→[700] (3.30→6.49:1), `dark.action.destructive` switched from white-on-red[500] (3.76:1) to dark-on-red[400] (8.9:1) to match the dark-mode bright-bg + dark-fg pattern. Filled-pill text contract documented: foreground = `theme.text.inverse` for all themes. (3) Colour-blindness independence audit — Viénot 1999 LMS simulation for deuteranopia, protanopia, tritanopia × 7 critical state pairs × 3 themes = 63 pairs. **Status: 59 distinct, 3 marginal, 1 indistinguishable.** The indistinguishable case (sunlight `feedback.syncing` vs `feedback.info` under tritanopia, ΔE=0.5) accepted with documented conditions: never co-present in UI, reinforced by icon + text, tritanopia is ~0.01% of the population. Critical pair `feedback.offline` vs `feedback.syncing` (FFIE-specific P2 load-bearing) verified distinct (ΔE > 15) across all 9 conditions. New library files: [src/lib/colorblind.ts](../../design-system-preview/src/lib/colorblind.ts) and [src/lib/color-audit.ts](../../design-system-preview/src/lib/color-audit.ts). Open follow-ups: introduce `border.strong` and `border.focus` semantic tokens (current `border.default` = gray[200] is decorative-only, doesn't clear 3:1); add a full-page dichromacy preview toggle; field-test sunlight theme on Julien's actual phones.
- 2026-05-28 — **StatusPill component ✓ v1.0** → [./design-system/components/StatusPill.md](./design-system/components/StatusPill.md) + reference impl at [../design-system-preview/src/components/ui/StatusPill.tsx](../../design-system-preview/src/components/ui/StatusPill.tsx) + preview at [/components/status-pill](http://localhost:3001/components/status-pill). Skill: `design-systems--component-spec` + `adaptive-interfaces--colour-independence` + `inclusive-interaction--feedback-and-status`. tokens.ts v0.5 → v0.6. **The P2 offline-first backbone** — every cached doc row, news card freshness indicator, and saving toast in the Julien flow leans on this component. 8 statuses (`success`, `warning`, `danger`, `info`, plus the P2 quartet `offline`, `syncing`, `stale`, `fresh`) × 2 variants (filled / subtle) × 3 sizes (sm 20 / md 24 default / lg 32 — only lg is tappable, reaches 44pt via 6pt hit-slop). **Color independence as a compile-time contract**: every status has a unique lucide icon (`WifiOff`, `RefreshCw`, `Clock`, `CheckCircle2`, `AlertTriangle`, `AlertCircle`, `Info`) — the icon prop defaults from a status→icon map and is non-negotiable. This is the mitigation for the COLOR_SYSTEM colorblind audit's marginal sunlight `syncing`↔`info` pair (ΔE=0.5 under tritanopia): the rotating `RefreshCw` vs static `Info` icons are radically different shapes regardless of color perception. **Token additions** — `theme.feedback.subtle.{success,warning,danger,info,offline,syncing,stale}.{bg, fg, border}` across all 3 themes. Light = soft tint (e.g. green[50] bg, green[800] fg, green[200] border). Dark = inverted (green[950] bg, green[300] fg, green[800] border). **Sunlight subtle = outlined** (white bg, saturated fg, saturated border) to preserve the P1 max-contrast-outdoors promise even in low-emphasis use. Discriminated TS union enforces `onPress` only valid when `size="lg"` (sm/md are signals not buttons). Syncing rotation kept under `prefers-reduced-motion: reduce` (rotation IS the affordance — without it "Sync…" reads as a static label); pulse opacity loop dropped. `fresh` status auto-fades 3s after mount (transient). `live={true}` adds `aria-live="polite"` for toast-style usage; NEVER applied to list-row pills (would flood SR). New default FR labels: Hors-ligne / Sync… / Périmé / À jour / Réussi / Attention / Erreur / Info — overridable via `children`. Preview renders: the canonical Julien doc-list with 4 P2 pills inline next to titles (`subtle sm`), full 8×2×3 status×variant×theme matrix, **live dichromacy filter toggle** (none / deuteranopia / protanopia / tritanopia) wired via SVG `<feColorMatrix>` (Brettel/Viénot 1999) — visually proves the icons survive when color doesn't, sizes row, tappable lg row, live-region demo (Save → "Sync…" → "Sauvegardé" sequence with `aria-live` re-announcement), fresh auto-fade demo, press log, 7 design contracts table. Type-check clean; all 16 routes 200. Open: extend `color-audit.ts` with 48 new `feedback.subtle.*` pairs; build Toast wrapping `lg filled` StatusPill (closes the parent half of Button `confirm="undo"`); i18n indirection for FR/EN label table; resolve `ds/` vs `ui/` folder drift (carried from Button).
- 2026-05-28 — **Button component ✓ v1.0** → [./design-system/components/Button.md](./design-system/components/Button.md) + reference impl at [../design-system-preview/src/components/ui/Button.tsx](../../design-system-preview/src/components/ui/Button.tsx) + preview at [/components/button](http://localhost:3001/components/button). Skill: `design-systems--component-spec`. tokens.ts v0.4 → v0.5. **First component to encode the P5 forgive-the-editor contract end-to-end** via two destructive `confirm` modes: `confirm="undo"` (fires immediately, parent owns the 60s undo toast — `motion.duration.undo`=60000ms) and `confirm="hold"` (press-and-hold for `motion.duration.confirm`=600ms with a live progress arc around the label; Esc cancels; release before 100% cancels). The TS prop API enforces invariants at compile time via discriminated unions — destructive without confirm = type error, iconOnly without ariaLabel = type error, iconOnly with children = type error. **Token additions:** `action.{primary,secondary,destructive}.{bgHover,bgPressed}` across all 3 themes (light: one ramp step darker; dark: one ramp step lighter — "more engaged" reads as bright on dark surfaces; sunlight: navy[900]→navy[950]→black for outdoors). New `action.secondary` slot — outlined, transparent bg, teal[700] border + fg in light. `primitives.motion.duration.loop`=1000ms for the spinner rotation. Existing consumers (`theme.action.primary.bg`/`.fg` reads in themes/page.tsx and color-audit.ts) unchanged — `.bg`/`.fg` kept as defaults, new state variants are siblings. 4 variants (primary / secondary / destructive / ghost) × 3 sizes (sm 40 / md 48 / lg 56 — md is P1 touch-target floor, NEVER compressed by density) × fullWidth | hug | iconOnly. States: default / hover (pointer only) / focus (`border.focus`, 2pt outline + 2pt offset, never suppressed) / pressed (bg→bgPressed, scale 0.97, 120ms `motion.fast`) / disabled (`opacity.disabled` 0.4, `aria-disabled`, focusable so SR users hear it) / loading (Loader2 icon with CSS spin animation, `aria-busy`, width unchanged, label opacity 0.4). `prefers-reduced-motion` drops the press scale transform but keeps the loading spinner (rotation IS the affordance) and keeps the hold-arc fill (safety mechanic). Color independence (P4) reinforced beyond color alone: `secondary` is OUTLINED (not just a different fill), `destructive` carries AlertTriangle as default `iconLeading`. lucide-react installed (Loader2 / ArrowRight / Download / Save / Trash2 / Search / Wifi / AlertTriangle used in preview). Preview page renders: density toggle, full variant×theme grid (each cell paints `surface.default` of the row's theme to validate adjacency), size row, state row (default / disabled / loading), width row (fullWidth + icon-only triple), the P5 two-mode destructive side-by-side, side-by-side density worked example with the active mode highlighted, live press-log terminal (last 8 events), and the 7-row design-contracts table tying each commitment to its enforcing file. Type-check clean; all 15 routes still 200. Open: RN parity (deferred to Stage 3 via `Pressable` + `react-native-reanimated` + `lucide-react-native`); component-level token slots emerge after the next component (likely StatusPill or IconButton); convention drift between `src/components/ds/` (Input) and `src/components/ui/` (Button) — pick one before the 3rd component lands; French label width test (Enregistrer hors-ligne fits md hug?); Storybook-on-device at Stage 3 kickoff.
- 2026-05-28 — **First component ✓ Input v1.0** → [./design-system/components/Input.md](./design-system/components/Input.md) + reference impl at [../design-system-preview/src/components/ds/Input.tsx](../../design-system-preview/src/components/ds/Input.tsx). Skill: `design-systems--component-spec` + `accessible-content--form-labelling`. Variants: text / search / password / email. Density-aware height contract (40 compact / 48 comfortable default / 56 spacious — comfortable matches sizes.touchTarget.primary). **Two new theme tokens introduced** — closes the follow-up flagged in COLOR_SYSTEM.md: `theme.border.strong` (gray[400] light / gray[500] dark / black sunlight — clears 1.4.11 non-text 3:1, used for load-bearing input outlines + table dividers under text) and `theme.border.focus` (brand.teal[600] light / brand.teal[300] dark / brand.navy[900] sunlight — the focus ring). Also added `theme.text.placeholder` (gray[500] / gray[500] / gray[700]) to distinguish hint text from body. Contrast catalog still 33/33 after additions. lucide-react wired for icons (Search, Eye/EyeOff, X). The component owns: focus-ring contract (2pt border + 2pt offset = 4pt visible indicator, 120ms `motion.duration.fast` transition), label/helper/error layout (real `<label>` with htmlFor, aria-invalid + role="alert" + aria-describedby), required marker (visible `*` + aria-required + html required), placeholder-is-never-a-label rule. New routes: `/components` (index) and `/components/input` (live theme + density toggles wrapped around a themed surface so dark + sunlight visibly re-skin every variant). 6 anatomy contracts and a 16-row token recipe table documented in the preview and the spec doc. Open: TextArea / Multiline, Numeric/OTP, autocomplete/suggestions popover (will be a separate SearchBar component wrapping Input, not a variant), Form + FormField wrappers — all deferred past v1.0.
- 2026-05-28 — **Scope policy clarified: website = design system only; screens = mobile only.** The product is a React Native mobile app for iOS + Android, built under `./mobile/`. The Next.js site under `./design-system-preview/` is **purely design-system documentation** — tokens, foundations, components. Screens belong in `./mobile/` only. Cleanup executed in this turn: deleted `design-system-preview/src/app/[locale]/screens/{splash,doc-library,page.tsx}` and `design-system-preview/src/components/screens/Splash.tsx`; removed `screens` / `splash` / `docLibrary` Nav entries; removed `Splash` and `SplashPreview` namespaces from `messages/{fr,en}.json`. The Splash + Doc Library spec docs at `./design-system/screens/*.md` survive as documentation. The mobile RN ports at `mobile/src/screens/{SplashScreen,DocLibraryScreen}.tsx` survive as the real product code. Two previous STATE entries are now partly inaccurate (they describe a web preview + mobile native combo for Splash and Doc Library) — kept verbatim below for traceability; the web halves no longer exist.
- 2026-05-28 — **App.tsx debug strip removed.** Replaced the floating design-preview debug strip (theme / density / network / flow chips + the `DEBUG` corner re-open handle in the bottom-right) with a clean root. `themeName` / `density` / `offline` now live as top-level consts at production defaults (light, comfortable, online) waiting for a real Settings screen to drive them. Cleaned: `showDebug` state, `lastTapped` state, `resetOnboarding`, `THEMES` / `DENSITIES` arrays, `DebugRow` / `Chip` helpers, all debug styles, and the `Pressable` / `StyleSheet` imports that were only used by the debug. `PublicPlaceholder` copy that referenced "barre de debug" updated. `npx tsc --noEmit` clean. The mobile app now reads as production scaffolding rather than a design-preview tool. Open: real Settings screen (deferred); biometric prompt + persistence will need a way to flip `offline` for testing — bring back a hidden gesture (e.g., 5-tap on the version stamp) or build a proper dev menu when needed.
- 2026-05-28 — **Splash v1.2 — text-free brand statement.** Removed every text element from the Splash: FFIE wordmark, federation full name, italic tagline, "Préparation…" status line, and `v0.6` version stamp. Removed the dark navy scrim from v1.1; replaced with a **bottom-up white LinearGradient** (transparent → 0% at top → transparent at 45% → 75% white at 82% → solid white at 100%) so the bottom edge fades into PathSelectionScreen's `surface.default` — the splash → onboarding handoff is now seamless, no visual seam at the route transition. The image (`home-banner-bg.jpg`) carries the brand on its own; the logo (white-card variant, keeps reading on the dark sky) + three bright-teal pulsing dots are the only foreground content. `accessibilityLabel` still reads "FFIE · {federation full name}" so screen-reader users still hear the federation identity even though the text isn't on-screen. Motion sequence simplified from 4 animated values to 2: logo enters at 0ms, loader enters at 280ms; both via `motion.duration.slow` 320ms `easing.decelerate`. Dot pulse + reduced-motion contracts unchanged. `npx tsc --noEmit` clean.
- 2026-05-28 — **Splash v1.1 + onboarding back-affordance audit ✓** → updated [./design-system/screens/Splash.md](./design-system/screens/Splash.md) §3 + native RN at [`mobile/src/screens/onboarding/SplashScreen.tsx`](../../mobile/src/screens/onboarding/SplashScreen.tsx). Per AGENTS.md, consulted https://docs.expo.dev/versions/v56.0.0/ before writing — `expo-router` is recommended for SDK 56 but the existing flow is a self-contained state machine in `OnboardingFlow.tsx` (no router needed at v1; deferred). **Splash bg → `mobile/assets/home-banner-bg.jpg`** (dusk view of Nice with electrical IoT icons overlaid — doubles as a brand statement). Implementation: `ImageBackground resizeMode="cover"` + `expo-linear-gradient` scrim navy[950] @ 0.55→0.78 alpha so every white element clears 4.5:1 against any region of the image. Text colors switch to white-on-dark constants (`#FFFFFF`, `rgba(255,255,255,0.78)`, `rgba(255,255,255,0.5)`); `FFIELogo themeName="dark"` forces the white-card variant so the navy block in the SVG stays legible. The dot loader uses `themes.dark.brand.accent` (bright teal) across all themes — reads on the navy image regardless of active theme. Solid `#080C1F` (navy[950]) backstop renders behind the image while the JPG decodes. All motion + reduced-motion contracts unchanged from v1.0. **Onboarding back-affordance audit:** OnboardingFlow has 4 interactive screens after splash — PathSelection (no back, correct: first interactive screen, splash is brand-only), MemberSignIn (Back wired), Discover (back via `onSignInInstead`), BiometricPrompt (**gap → fixed**: added `onBack` prop + a top-left `ChevronLeft Retour` row that returns to `sign-in` so users can correct their identifier before binding biometrics). `npx tsc --noEmit` clean.
- 2026-05-28 — **Splash screen ✓ v1.0 (mobile only — web preview removed above)** → [./design-system/screens/Splash.md](./design-system/screens/Splash.md) + native RN port at [`mobile/src/screens/SplashScreen.tsx`](../../mobile/src/screens/SplashScreen.tsx). Skills: `design-systems--component-spec` + `interaction-design--animation-principles`. **First mobile-native screen built directly in `./mobile/`.** Federation identity surface: FFIE logo (sourced from ffie.fr, copied to `mobile/assets/logo-ffie.svg`) + wordmark + full federation name + the canonical French tagline "Construisons l'électricité, ensemble." while the offline cache rehydrates. **Three-rule motion contract:** (1) sequential fade-in @ 0/120/280/440ms with `motion.duration.slow` (320ms) + `easing.decelerate` — guides the eye logo → identity → tagline → loader; (2) three pulsing brand.accent dots cycling 0.4↔1.0 opacity over 1200ms with 160ms phase offsets between dots — calmer than a spinner, matches federation tone; (3) `prefers-reduced-motion: reduce` (web) / `AccessibilityInfo.isReduceMotionEnabled()` (RN) hard-switches every delay to 0 and removes the dot pulse — splash still works. Token recipe entirely composed from existing tokens (display.md for FFIE wordmark, eyebrow for federation name, body.lg italic for tagline, brand.accent for dots, text.brand for the wordmark color which uses brand.navy[700] in light/sunlight and brand.teal[300] in dark). **Theme-aware logo treatment**: in dark mode the logo sits inside a white card (`inset.compact` + `radii.md`) so the SVG's navy block stays legible against gray[950] — the one place we accept a visible exception to "edge-to-edge on surface." A11y: announced as a single image with label "FFIE · {federation full name}"; tagline + wordmark + federation name also rendered as real Text nodes so screen-reader navigation works; loader is `aria-hidden` (status line carries the information). No focusable elements — host orchestration auto-dismisses on `min(actualLoad, 3000ms)` — the 3s floor preserves brand recognition on fast cold starts. Locale: French tagline is canonical (matches ffie.fr); English tagline is a working approximation; federation name is a French proper noun and stays in French in both locales. RN/web parity intentionally tight — same timings, same token recipe, same aria role + label, just `Animated.timing` + `expo-image` swapped for CSS animations + `next/image`. All 19 routes × 2 locales = 38 endpoints all 200. Open: real cache-rehydration hook (host orchestration lands with the App Shell); 3-second floor enforcement (same); NBSP in federation name in messages catalogs; pre-bundled OS-level launch image (iOS LaunchScreen + Android `splashscreen_image`) that matches the dynamic Splash within a frame so handoff is invisible.
- 2026-05-28 — **Card component ✓ v1.0** → [./design-system/components/Card.md](./design-system/components/Card.md) + reference impl at [../design-system-preview/src/components/ds/Card.tsx](../../design-system-preview/src/components/ds/Card.tsx) + preview at [/components/card](http://localhost:3001/components/card). Skill: `design-systems--component-spec`. **The final primitive composition before Stage 3.** One spec serves four very different surfaces by switching `density` + `variant` only: news feed item (Julien P1+P2, comfortable+default), document library row (P2+P3, comfortable+default+`<CardRow>`), back-office data row (Sylvie P5, compact+outlined+padding:0+grid rows), public landing block (Karim+Léa P6+P7, spacious+raised). **No new tokens introduced** — Card composes everything already in tokens.ts. Sub-components describe semantic regions, not layout: `Card`, `CardHeader`, `CardEyebrow`, `CardTitle`, `CardDescription`, `CardMeta`, `CardContent`, `CardFooter`, `CardActions` (margin-left:auto right-align slot), `CardMedia`, `CardRow`. **The P2 stale contract is encoded inline:** `stale={true}` dims the inner content area to `opacity.stale` (0.6) with 200ms fade — but the Card itself stays at full opacity (so border + surface read crisply), AND the StatusPill is exempt (caller renders it in an absolutely-positioned overlay above the dim — the user must SEE the status). **Density contract reaffirmed:** Card padding shrinks/grows with density (8/16/24) but interactive primitives inside (Button, Input, StatusPill) keep their own size contracts — `density="compact"` lets Sylvie see more rows; it never steals touch area from actions. 4 variants: default (`elevation.sm`), raised (`elevation.md`), outlined (`border.strong` 3:1, no shadow — for data tables under text), flat (`surface.subtle`, no shadow — for embedded contexts). Sunlight emits no shadow regardless of variant (borders carry elevation outdoors per the theme contract). Theme inheritance works through both inline styles (when `themeName` is explicit) and CSS variables (default — via the shadcn HSL bridge), so a Card in a Modal portal still renders the active theme. Accessibility: Card is NOT a button by default — `interactive` only adds hover lift + cursor; callers wrap with `<a>` or `<button>` for real interactivity (stretched-link pattern documented). `<CardTitle>` is a real `<h3>` (polymorphic `as` deferred). Preview demonstrates all 4 canonical compositions plus the stale state and a back-office table built from compact Cards in a CSS grid. **6 components shipped: Input, Button, StatusPill, Toast, Modal, Card.** Stage 2 component layer is now complete. Open: `asChild` Slot pattern for `<Link>` composition; polymorphic `as` on CardTitle; `<CardLinkOverlay>` helper for the stretched-link pattern; Skeleton variant deferred to Stage 3; real-device sunlight test of the borderless-shadow contract bundled with existing field-tests; RN sibling at Stage 3.
- 2026-05-28 — **Modal component ✓ v1.0** → [./design-system/components/Modal.md](./design-system/components/Modal.md) + reference impl at [../design-system-preview/src/components/ds/Modal.tsx](../../design-system-preview/src/components/ds/Modal.tsx) (FFIE-aliased re-exports of shadcn Dialog) + [`ConfirmModal.tsx`](../../design-system-preview/src/components/ds/ConfirmModal.tsx) + preview at [/components/modal](http://localhost:3001/components/modal). Skill: `design-systems--component-spec`. **First component to encode the full P5 forgive-the-editor contract end-to-end with the new sonner foundation.** The cardinal rule: **the modal IS the confirmation step**. No hold-arcs inside, no typed "DELETE" gates, no compound second confirms — one decision, one optimistic apply, one 60s undo window. Two layers: (a) primitives — `Modal`, `ModalContent`, `ModalHeader`, `ModalTitle`, `ModalDescription`, `ModalFooter`, `ModalClose`, `ModalTrigger`, `ModalPortal`, `ModalOverlay` re-exported from the shadcn Dialog at `ui/dialog.tsx` (Radix + the FFIE CSS-variable bridge); (b) **ConfirmModal** convenience — 3 semantic variants (`info` / `confirm` / `destructive`) each with matching feedback color + lucide icon (Info / CheckCircle2 / AlertTriangle), 38pt tinted-circle wrapper, sensible default labels from the `Modal` namespace in messages/{fr,en}.json. Destructive variant uses the existing Button at `variant="destructive" confirm="undo"` — fires `onPress` immediately, ConfirmModal closes the modal, parent owns the optimistic apply + sonner toast with `duration: 60_000` and an Undo action that reverts. **Server-side write is NOT in onConfirm — it's at toast dismiss** (the 60s is for real undo, not optimistic UI with rollback). Cancel is first in DOM footer order so Enter dismisses the safe path; on mobile the footer is `flex-col-reverse` so the destructive button sits closer to the thumb. Radix handles focus trap, aria-modal, aria-labelledby/describedby, inert background, Esc-to-close, body scroll lock. `motion-reduce:animate-none` neutralizes enter/exit. Theme propagation in portals works via `<html data-theme>` (shadcn bridge) — Tailwind utilities resolve to the active theme HSL automatically; ConfirmModal still passes explicit `themeName` to its inner Buttons because Button consumes via inline styles, not CSS vars. **`modal={true}` opt-in** suppresses outside-click + Esc — reserved for true blocking errors. i18n: 4 default labels (cancel / confirm / delete / acknowledge) in the `Modal` namespace; per-usage strings are caller-supplied. The /components/modal preview demonstrates all 3 variants live with a working 60s undo toast (sonner), a strikethrough on the optimistically-deleted "NF C 15-100 §7.2" row that reverts on Undo, and a 6-row design-contracts table. Bumps the components list to **4 components shipped** (Input, Button, StatusPill, Modal). All 16 routes × 2 locales = 32 endpoints all 200. Open: Sheet variant (mobile slide-up); multi-step WizardModal; per-theme shadow drop in sunlight (low-priority); explicit `onOpenAutoFocus` to lock initial focus on Cancel regardless of children reorder; RN sibling via `react-native-bottom-sheet` + a sonner-equivalent toast at Stage 3. `motion.duration.undo` (60000ms) now has a real consumer end-to-end.
- 2026-05-28 — **shadcn/ui bridged ✓** → [./design-system/SHADCN_INTEGRATION.md](./design-system/SHADCN_INTEGRATION.md). Skill: `shadcn-ui`. Component foundation now sits on Radix UI primitives + shadcn's CSS-variable theming pattern. **The visual design is unchanged** — tokens.ts remains source of truth. Bridge is a single block in globals.css that maps shadcn's role tokens (`--background`, `--foreground`, `--primary`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, `--radius`) **in HSL form** under three selectors — `[data-theme="light"|"dark"|"sunlight"]` — and `ThemeProvider` propagates the active theme to `<html data-theme>` so the variables cascade into Radix portals (Dialog overlay, sonner toaster). Tailwind config exposes them as utilities (`bg-primary`, `border-input`, `ring-ring`, etc.); `darkMode: ["class", '[data-theme="dark"]', '[data-theme="sunlight"]']` activates `dark:` utilities under both dark and sunlight. Installed: `clsx`, `tailwind-merge`, `class-variance-authority`, `tailwindcss-animate`, `@radix-ui/react-dialog`, `@radix-ui/react-slot`, `sonner`. New: [`src/lib/utils.ts`](../../design-system-preview/src/lib/utils.ts) (cn() helper), [`src/components/ui/dialog.tsx`](../../design-system-preview/src/components/ui/dialog.tsx) (foundation for Modal — P5 destructive confirm + 60s undo will compose this), [`src/components/ui/toaster.tsx`](../../design-system-preview/src/components/ui/toaster.tsx) (sonner wrapper, mounted in root layout — foundation for the 60s undo toast). `components.json` scaffolded at repo root for future `npx shadcn add` runs (style: new-york, baseColor: slate, cssVariables: true). **Adoption policy:** hand-rolled Input (`src/components/ds/`) + Button (`src/components/ui/`) stay as-is — they encode FFIE-specific contracts not naturally expressed via shadcn defaults. Future components: Radix primitive where one exists, FFIE wrapper for the contract on top. Open: naming drift between `ds/` and `ui/` (resolve before next component); evaluate migrating hand-rolled Button to shadcn-cva pattern; per-theme shadow drop in sunlight (Radix Dialog currently still emits shadow-lg, acceptable for now). All 15 routes still 200; 33/33 contrast still passes.
- 2026-05-28 — **i18n wired ✓ — French primary, English at `/en/*`** → [./design-system/I18N.md](./design-system/I18N.md). Skill: implicit (no `i18n` skill in the bundle). Library: `next-intl` v4.12 — canonical for Next.js App Router. **Default locale: French** (FFIE is a French federation; Julien and Sylvie both work in French). **URL strategy:** `localePrefix: "as-needed"` — `/` = French (no prefix, canonical), `/en/...` = English. Requesting `/fr/*` 307-redirects to the prefix-stripped URL and sets `NEXT_LOCALE=fr` cookie. Routes restructured under `src/app/[locale]/...`; the `[locale]` segment captures the active locale and `generateStaticParams()` pre-renders both variants. New files: [`src/middleware.ts`](../../design-system-preview/src/middleware.ts) (MUST be under `src/`, not project root, because this project uses a src directory), [`src/i18n/routing.ts`](../../design-system-preview/src/i18n/routing.ts), [`src/i18n/request.ts`](../../design-system-preview/src/i18n/request.ts) (loads message catalog, sets `timeZone: "Europe/Paris"`), [`src/i18n/navigation.ts`](../../design-system-preview/src/i18n/navigation.ts) (locale-aware `Link`, `useRouter`, `usePathname`), [`messages/fr.json`](../../design-system-preview/messages/fr.json) and [`messages/en.json`](../../design-system-preview/messages/en.json) (Nav + Overview namespaces seeded), [`src/components/LocaleSwitcher.tsx`](../../design-system-preview/src/components/LocaleSwitcher.tsx) (client radiogroup mounted in Nav). next.config.mjs wrapped with `createNextIntlPlugin("./src/i18n/request.ts")`. Nav is now an async Server Component using `getTranslations("Nav")`. ICU MessageFormat used for variables, dates, plurals. French typographic conventions documented in I18N.md §5 (non-breaking space before `:` `;` `?` `!` `«»`; comma as decimal; `28 mai 2026` date format; lowercase month names). Per TYPOGRAPHY.md cross-ref: French is +15-25% longer than English on average → the 200% Dynamic-Type test at /foundations/typography is effectively English at 230% when toggled to FR — every layout that passes EN-200% must be re-validated in FR-200%. Verified: 15 routes × 2 locales = 30 endpoints all 200. Translations visibly render — `/` shows "Système de design" + "Couleurs"; `/en` shows "Design System" + "Color". Open: migrate remaining page strings to t() calls one page at a time (today only Nav + Overview namespace seeded); translator workflow decision with FFIE (FFIE comms team owns FR vs translation memory like Crowdin); date/number formatting on the spacing + typography pages; French at 200% field-test on Julien's phones; verify Museo Sans license includes `latin-ext` when FFIE confirms.
- 2026-05-28 — **Toast component ✓ v1.0** → [./design-system/components/Toast.md](./design-system/components/Toast.md) + reference impl at [../design-system-preview/src/components/ui/Toast.tsx](../../design-system-preview/src/components/ui/Toast.tsx) + responsive host upgrade at [../design-system-preview/src/components/ui/toaster.tsx](../../design-system-preview/src/components/ui/toaster.tsx) + preview at [/components/toast](http://localhost:3001/components/toast). Skill: `design-systems--component-spec` + `inclusive-interaction--feedback-and-status` + `inclusive-interaction--motion-sensitivity`. **Closes the parent half of the Button `confirm="undo"` contract** — `toast.undo(message, { onUndo })` defaults to `motion.duration.undo` (60 000 ms), composes an `info` (or `success`) StatusPill as the leading mark, and renders an inverse "Annuler" action. **Toast is StatusPill's bigger sibling for transient surface communication** — every toast renders a `size="md" variant="filled"` StatusPill, so the colour-independence backbone (shape+icon+label trio per P4) carries through. API surface: imperative `toast.{success, warning, danger, info, offline, syncing, stale, fresh, undo, dismiss}` over `sonner.toast.custom()`. Defaults per status (Toast.md §3) — `danger` + `syncing` stay open until manually dismissed (`Infinity`), `success` + `info` + `stale` 4s, `warning` + `offline` 6s, `fresh` 3s. **Promote-by-id pattern** for long-running actions: `const id = toast.syncing("Sauvegarde…"); toast.success("Sauvegardé", { id })` cross-fades content in place via the same sonner row. `danger` toasts upgrade to `role="alert"` + `aria-live="assertive"`; every other status stays at `role="status"` + `aria-live="polite"`. Toast NEVER steals focus on mount. **Host upgrade** ([`toaster.tsx`](../../design-system-preview/src/components/ui/toaster.tsx)) — responsive position policy (top-center < 768px for thumb-reachable on Julien's worksite phone; bottom-right ≥ 768px for Sylvie's back-office), `visibleToasts={3}`, `pauseWhenPageIsHidden`, `expand` stack-on-hover, `gap=8` + `offset=16`. Re-mounts on breakpoint cross via `key={position}` since sonner reads position only at mount. The host now sets ONLY position + stacking + theme; visual rendering happens inside `toast.custom()` via the FFIE `ToastRow`. **Theme resolution** uses `document.documentElement[data-theme]` set by ThemeProvider (Toast.tsx) — sonner mounts its own root outside React's tree, so we can't `useTheme()` directly inside the custom row; reading the DOM attr threads the live theme into toasts cleanly. Preview at [/components/toast](http://localhost:3001/components/toast) demonstrates: each of the 8 statuses fired with one click, **destructive Button + `toast.undo` end-to-end** (with a working counter that decrements on Undo), promote-by-id (`syncing → success` and `syncing → danger` variants), stacking (fires 5 toasts at 250ms intervals — last 3 visible, rest queued), long-title truncation, long-description 4-line clamp, position-policy explainer, event log, and a 9-row design-contracts table. Nav + i18n updated — `statusPill` + `toast` keys added in both messages/{fr,en}.json; `/components/status-pill` was missing from the nav prior to this pass and is now wired. Components index moves Toast from "planned" to **live v1.0**; only Card remains in the planned list. tokens.ts version unchanged (v0.6) — Toast consumes existing slots only (`surface.raised`, `text.{default, muted}`, `border.subtle`, `radii.lg`, `elevation.lg`, `motion.duration.undo`). Open follow-ups: RN parity at Stage 3 (`react-native-toast-message` or Reanimated layer); `toast.promise(p, { loading, success, error })` helper (defer to v1.1 — promote-by-id covers the same ground today); component-level token slots emerge after we field-test in app screens; queue cap decision; `ds/` vs `ui/` folder drift still carried forward.
- 2026-05-28 — **Stage 3 kickoff — Screen 1 ✓ Doc Library (Julien)** → [../design-system-preview/src/app/[locale]/screens/doc-library/page.tsx](../../design-system-preview/src/app/%5Blocale%5D/screens/doc-library/page.tsx). Skill: implicit (composes Input + StatusPill + lucide). **The canonical P2 scenario in real pixels** — what Julien sees when an apprentice asks for the right NF C 15-100 clause on a worksite. iPhone 12 frame (390×844pt), French copy throughout (the app's canonical language), 10 mixed-state docs (5 offline, 1 syncing, 1 stale, 1 fresh, 2 offline for variety) drawn from his real regulatory world: NF C 15-100, NF C 14-100, Décret tertiaire, RE 2020, Consuel circulaires, Qualifelec, RAGE 2012, OPPBTP, NF EN 50171. **Eight design contracts honored** and surfaced as a recite-able table beneath the screen: search-as-front-door with `stack.section` 40pt clearance (P3); rows ≥48pt with `inset.default` padding (P1+P4); offline banner that surfaces cached availability instead of dead-ending (P2); per-row StatusPill `subtle sm` carrying state via shape+icon+label (P2+P4); whole row is a single `<button>` with no nested focusable (StatusPill `sm` is non-tappable by spec — clean a11y, the nested-focusable problem resolves later inside the Card spec); sunlight theme honored end-to-end (every text/border/bg reads at AAA in glare); stale row remains tappable to refresh as a distinct hit area; mobile gutter = 16 per `gutter.mobile`. **Preview controls** wired live: theme (light/dark/sunlight), density (comfortable/compact/spacious), network (5G/offline — toggles the offline banner), and dichromacy filter (none/deuteranopia/protanopia/tritanopia via SVG `feColorMatrix` Brettel-Viénot). Plus a live tap log confirming row taps fire. **Tappable row inlined intentionally, not specced** — the row container is just a themed `<button>` with `surface.subtle` press feedback and the same focus-ring contract as Button. Formalizing it as a component would be premature abstraction (single point of use); it gets retrofitted to whatever Card decides about nested-focusable behaviour when Screen 3 lands. Routes updated: new `/screens` index + `/screens/doc-library`; nav extended with screens section + i18n keys (`screens` → "Écrans"/"Screens", `docLibrary` → "Bibliothèque — Julien"/"Doc library — Julien"). 14/14 routes still 200. Open follow-ups: bind Search input to the proper `Input` component (currently a screen-local inline implementation because Input v1.0 is web-form-shaped — needs a "mobile-shaped" variant or a dedicated `SearchBar` wrapper); list virtualization (10 docs doesn't need it; news feed will); the in-doc search teaser ("section câble 32A" — search inside content, not just titles); the "download for offline" CTA for not-cached docs (out of scope for v1 of this screen — separate flow when Search returns server-side results not in cache).
- 2026-05-28 — **Spacing system ✓** → [./design-system/SPACING.md](./design-system/SPACING.md). Skill: `ui-design--spacing-system`. tokens.ts v0.3 → v0.4. **TOKENS.md §9.1 resolved — 4pt base locked** (hybrid step: 4pt 0→16, 8pt 16→80). Rationale ranked in the doc against an 8pt-only alternative; the deciding factor was Sylvie's back-office tables (P5) needing 8pt + 12pt row paddings, plus 44/48pt touch targets not fitting an 8pt-only grid. Primitive `space` scale untouched. **New semantic spacing layer** added as a `semantics` export (peer to `themes`, not theme-keyed): four families × density-aware → `inset` (padding inside containers — none/tight/compact/default/comfortable/spacious/hero), `stack` (vertical rhythm — none/tight/snug/default/loose/section/page), `inline` (horizontal row spacing, with `inline.snug`=8pt documented as the MIN gap between two adjacent tappable targets per P4), `gap` (grid/flex), `gutter` (page-level horizontal margin per breakpoint — mobile 16 / tablet 24 / desktop 32 / wide 40). **Three density modes**: comfortable (default — mobile member-facing P1), compact (back-office tables — Sylvie P5), spacious (public landing — P7). Implemented as a one-step shift via `withDensity(family, token, mode)` helper exported from tokens.ts — clamps at scale boundaries. Touch targets and `inline.snug` minimum gap are explicit floors that density NEVER shifts (P4 — accessibility is a floor not a ceiling). **8 persona-anchored design contracts** surfaced (mobile gutter = 16, primary target = 48 never compressed, status pills get inline.snug from text, search bar gets stack.section above results, destructive modals get inset.comfortable, etc.) — all rendered as a recite-able rule table in the preview. Rebuilt [/foundations/spacing](http://localhost:3001/foundations/spacing): primitive scale, semantic families with density-shift live indicators, breakpoint-aware gutter table, density toggle wired to a worked example (same card rendered at all 3 modes side-by-side), the 8 contracts table. Type-check + all 11 preview routes still 200. Open follow-ups: component-tier spacing tokens emerge with `design-systems--component-spec`; density-aware Tailwind plugin for back-office (defer until first back-office screen); ESLint rule against raw `primitives.space.*` in components; field-test `gutter.mobile` on Julien's actual phones (bundled with sunlight field-test).

## Active artifacts
- PRD: ./project/brief/FFIE_PRD_v0.2_EN.docx.pdf (5 pages, v0.2 Draft, 30/04/2026)
- Personas CSV: ./project/brief/FFIE_WBS Fonctionnel - 0_Persona.csv (4 personas)
- Epics CSV: ./project/brief/FFIE_WBS Fonctionnel - 1_Epic.csv (12 epics, 10 Phase 1 + 2 Phase 2)
- Stories CSV: ./project/brief/FFIE_WBS Fonctionnel - 2_Stories.csv (29 stories)
- References folder: ./project/brief/references/ (empty)
- Design brief: ./project/brief/DESIGN_BRIEF.md
- Personas: ./project/brief/PERSONAS.md
- Design principles: ./project/brief/DESIGN_PRINCIPLES.md
- Token taxonomy: ./project/design-system/TOKENS.md (v0.1 — taxonomy)
- Color system documentation: ./project/design-system/COLOR_SYSTEM.md (v1.0 — methodology, ramp audit, cross-theme contrast catalog, colour-blindness independence audit)
- Typography documentation: ./project/design-system/TYPOGRAPHY.md (v1.0 — scale audit, 15-role table, dynamic-type contract, locale considerations)
- Spacing system documentation: ./project/design-system/SPACING.md (v1.0 — 4pt base resolved, semantic layer inset/stack/inline/gap/gutter, 3 density modes, 8 persona contracts)
- shadcn integration: ./project/design-system/SHADCN_INTEGRATION.md (v1.0 — Radix UI + CSS-variable bridge to FFIE theme tokens)
- i18n documentation: ./project/design-system/I18N.md (v1.0 — next-intl, FR default, /en prefixed, French typographic conventions, migration plan)
- Modal component spec: ./project/design-system/components/Modal.md (v1.0 — primitives + ConfirmModal, 3 variants, P5 destructive-with-undo contract end-to-end)
- Card component spec: ./project/design-system/components/Card.md (v1.0 — composition primitive for news feed / doc library / back-office / landing surfaces; P2 stale contract + density contract enforced)
- Splash screen spec: ./project/design-system/screens/Splash.md (v1.0 — federation identity surface, web + RN parity, 3-rule motion contract, reduced-motion + sunlight rules)
- Input component spec: ./project/design-system/components/Input.md (v1.0)
- Button component spec: ./project/design-system/components/Button.md (v1.0 — 4 variants, 3 sizes, P5 confirm=undo|hold contract, lucide-react locked)
- StatusPill component spec: ./project/design-system/components/StatusPill.md (v1.0 — 8 statuses × 2 variants × 3 sizes, P4 colour-independence via shape+icon+label, P2 offline/syncing/stale/fresh backbone)
- Toast component spec: ./project/design-system/components/Toast.md (v1.0 — 8 statuses + toast.undo, parent half of Button confirm="undo" with 60s window, top-center mobile / bottom-right desktop, promote-by-id, role=alert for danger)
- Canonical token module (source of truth): ./project/design-system/tokens.ts (v0.6 — brand + a11y patched + textStyles + semantic spacing + withDensity helper + border.strong/focus + text.placeholder + action state variants (bgHover/bgPressed) + secondary action + motion.duration.loop + feedback.subtle.{name}.{bg,fg,border}, 3 themes)
- Live preview site: ./design-system-preview/ — `cd design-system-preview && npm run dev` → http://localhost:3001
- First user flow: ./design-system-preview/src/app/flows/julien-find-doc/page.tsx (Julien · find a regulatory doc on a worksite)
- First screen (Stage 3) — web preview: ./design-system-preview/src/app/[locale]/screens/doc-library/page.tsx (Julien · doc library, the P2 flow in real pixels — iPhone 12 frame, 10 French regulatory docs, all controls live)
- **Mobile app shell**: ./mobile/ — Expo SDK 56 + RN 0.85 + TypeScript. `cd mobile && npm start` then scan the QR with Expo Go on a physical phone. Imports the canonical [tokens.ts](./design-system/tokens.ts) directly via Babel `module-resolver` alias + Metro `watchFolders` (hot reload across the FFIE↔mobile boundary). Ships the doc-library screen ([./mobile/src/screens/DocLibraryScreen.tsx](../mobile/src/screens/DocLibraryScreen.tsx)) with the RN-ported StatusPill ([./mobile/src/components/ui/StatusPill.tsx](../mobile/src/components/ui/StatusPill.tsx)) and an on-device debug panel for theme/density/offline toggles. Lucide-react-native for icons; react-native-svg under the hood; react-native-safe-area-context for SafeAreaView. Next ports: Button, Input, Modal, Toast (deferred until first screen that needs them on device).
- FFIE logo asset: ./design-system-preview/public/logo-ffie.svg (sourced from ffie.fr)

## Open questions (from PRD)
- Opportunity / market positioning — section 2.3 marked TBC
- Member persona age range and field research basis — TBC
- Pain-point validation — both member pain points marked TBC (assumed, not researched)
- Acceptance criteria for the 3 core user stories US-01/02/03 — TBC
- Post-Phase 2 "Could" features — TBC with FFIE
- Calculator list (Phase 2) — TBC with FFIE
- Brand guidelines / visual identity reference — not provided
- Existing FFIE member-management API spec — referenced but not attached

## Next step
**Screen 2 OR Card — pick.** Two valid paths from here, both ~equally sized:

**Path A — Screen 2: Doc Detail + Save/Remove offline** (continue the Julien flow). Composes Button `confirm="undo"` + Toast `toast.undo` end-to-end for the first time — the P5 contract finally tested in real screen context. Header shows the syncing StatusPill during background refresh; body renders the doc viewer; bottom action bar carries "Sauvegarder hors-ligne" or "Retirer du hors-ligne" depending on cache state. Toast fires "Retiré du hors-ligne" with a 60s Undo. This validates the P5 loop in a real flow before Card adds composite complexity.

**Path B — Card (first composite).** Spec `project/design-system/components/Card.md` + reference impl + preview. Resolves the **nested-focusable a11y problem** (focusable card vs internal focusable Button — pick one, no double-focus) that Screen 3 (news feed) will need. Retrofits the inline DocRow in Screen 1 to use the canonical Card pattern. Unblocks Screen 3.

**Recommendation: Path A first.** Screen 2 is the closing argument for the P5 contract — without it, the Modal + Toast work is unproven in context. Card can wait until Screen 3, where it's actually load-bearing (news cards genuinely are vertical cards with image + title + lede + footer, where DocRow is just a list-item that happens to look like a card).

Carry-forward open items: drift between `src/components/ds/Input.tsx` and `src/components/ui/{Button,StatusPill,Modal,Toast}.tsx` (resolve before Card lands); extend `color-audit.ts` with the 48 new `feedback.subtle.*` pairs; the doc-library Search field currently uses an inline mobile-shaped input — formalize as a `SearchBar` wrapper around Input (or a mobile Input variant) once a second screen demands it.
